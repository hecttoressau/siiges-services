const isFrontPasswordRight = require('./is-front-password-right');
const signToken = require('./sign-token');
const verifyToken = require('./verify-token');

module.exports = {
  isFrontPasswordRight,
  signToken,
  verifyToken,
};
